{
  "name": "regulation-explorer",
  "version": "1.0.0",
  "description": "JS test harness for the regulation browser (parsers live in tools/parsers/ and are Python).",
  "private": true,
  "scripts": {
    "test": "pytest -q && node --test tests/*.test.js",
    "test:py": "pytest -q",
    "test:js": "node --test tests/*.test.js"
  },
  "devDependencies": {
    "jsdom": "^24.1.3"
  }
}
